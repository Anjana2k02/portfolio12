/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Enables static export
    basePath: '/portfolio12', // Add this if your repository is named 'portfolio12'
  };
  
  export default nextConfig;
  